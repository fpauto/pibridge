#!/bin/bash
# PiBridge CLI Wrapper Script
# Allows running pibridge commands without pip installation

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

exec python3 -m pibridge.cli "$@"